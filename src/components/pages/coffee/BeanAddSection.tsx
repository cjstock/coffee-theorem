import { motion } from "framer-motion";

const BeanAddSection = () => {
    return (
        <motion.div className="border-2 border-dashed rounded-lg p-2">
            <h1>Add Info</h1>
        </motion.div>
    );
};

export default BeanAddSection;
